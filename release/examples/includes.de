// такой конструкцией можно подключать модули
include" units\structures\arr.de"
// при этом если кто-то попытается подключить модуль повторно этим же методом,
// реально подключения не произойдёт
include" units\structures\arr.de"
// подключать можно и вычислив строку на стеке
str" units\" str" windows.de" str+ included
