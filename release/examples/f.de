// 2011.03.07 # Doj
// Пример работы с сопроцессором

// инициализируем работу с сопроцессором
finit

// вычисляем Round(Y + R*Sin(Angle*pi/180))
// : ex1 ( yra-i) w>f [ fpi 180 w>f f/ ] fliteral* f* fsin w>f f+ f>w ;
