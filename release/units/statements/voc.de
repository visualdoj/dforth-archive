// 2011.04.26 # Doj
// Вспомогательные команды для работы с пространствами имён и словарями.

: context-drop context> drop ;

: cs context> dup . context> dup . context< context< cr ;

: shortnamespace ( "name") create vocabulary-new dup target< dup context< , immediate 
  does> @ context< dembro-step context> drop ;
// : shortnamespace ( "name") namespace 
// does> ( "name") dup . @ ." HERE" dup . context< dembro-step ." hello" context-drop ." bye" ; )
